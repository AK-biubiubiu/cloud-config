spring:
  profiles:
    active:
    - dev

---
server:
  port: 8081

spring:
  profiles: dev
  application:
    name:  cloud-config-dev
    
---
server:
  port: 8082

spring:
  profiles: test
  application:
    name:  cloud-config-test
